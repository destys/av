import { create } from "zustand";

const useOrderDetailsModal = create((set) => ({
  isOpen: false,
  onOpen: () => set({ isOpen: true }),
  onClose: () => set({ isOpen: false }),
}));

export default useOrderDetailsModal;
